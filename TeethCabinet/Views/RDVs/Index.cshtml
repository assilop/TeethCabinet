@model IEnumerable<TeethCabinet.Models.RDV>

@{
    ViewBag.Title = "Index";
}

<h2>RDVs</h2>

<div>
    @using (Html.BeginForm("Index", "RDVs", FormMethod.Get))
    {

        @Html.RadioButton("searchBy", "Motif", true) <text> Motif</text>
        @Html.RadioButton("searchBy", "patient") <text> patient</text>
        @Html.RadioButton("searchBy", "personnel") <text> personnel</text><br />
        @Html.TextBox("search", null, new { @class = "form-control" });
        <button type="submit" class="btn btn-primary">Chercher</button>
    }

</div>
<br />


<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateHeureRdv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Motif)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.Nom) patient
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personnel.Nom) personnel
        </th>
        <th></th>
    </tr>

    @if (Model.Count() == 0)
    {
        <tr>
            <td colspan="4">Aucune ligne ne corresond aux criteres de recherche</td>
        </tr>
    }
    else
    {
        foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.DateHeureRdv)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Motif)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Patient.Nom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Personnel.Nom)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.RdvID }, new { @class = "btn btn-warning btn-sm" })
                    @Html.ActionLink("Details", "Details", new { id = item.RdvID }, new { @class = "btn btn-info btn-sm" })
                    @Html.ActionLink("Delete", "Delete", new { id = item.RdvID }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }

    }

</table>

<p>
    @Html.ActionLink("Creer nouveau", "Create", null, new { @class = "btn btn-primary" })
</p>






